# 2026-06-07 Round 17 Evaluation

## Scope
- Used the previous report's audit target: add a real-write audit filter preset for integration activity logs.
- Rechecked activity filtering, details JSON behavior, UI filter state, smoke coverage, tests, README notes, and existing pagination behavior.

## Scores
- Functionality: 9.8/10
- UI/design: 9.0/10
- Security/privacy: 9.3/10
- Performance: 9.0/10
- Tests: 9.9/10
- Documentation: 9.6/10
- Overall: 9.4/10

## Findings And Fixes
- Finding: Activity details recorded `dryRun`, but operators could not filter actual writes directly.
- Fix: Added `dry_run=true/false` query support to `GET /api/integration-activities`.
- Finding: UI required manual selection across several filters to audit live external writes.
- Fix: Added `Real-write audit` preset and an `All/Dry-run/Actual writes` filter control.
- Finding: CDP smoke did not verify actual-write audit query behavior.
- Fix: Added a `realWriteAuditApi` smoke check for `event_type=integration_profile.write&dry_run=false`.
- Finding: README did not mention the audit preset.
- Fix: Added a current-state note for `dry_run` filtering and `Real-write audit`.

## Review Rounds
- Round 1: Inspected activity serialization, log details, API filters, frontend filter state, and tests. Added `dry_run` filter and backend assertions.
- Round 2: Rechecked UI preset behavior, filter reset behavior, pagination compatibility, smoke coverage, build output, and README note. No unresolved regression found.

## Verification
- `python -m py_compile backend/app/main.py`: pass.
- `npm run verify:fastapi`: pass.
- `npm --prefix frontend run build`: pass, with the existing lucide `use client` bundler warning only.
- `APP_URL=http://127.0.0.1:5173 node scripts/verify-ui-cdp.mjs`: pass.

## Next Work Targets
1. Normalize remaining mojibake text in older README/demo content.
2. Add database indexes for high-volume activity and knowledge source queries.
3. Add per-automation run history pagination in the UI.
4. Add explicit UI affordance for exporting audit logs as JSON/CSV.
