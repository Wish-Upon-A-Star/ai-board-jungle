# Round 34 Evaluation - CDP Smoke Preflight

- Scope: CDP smoke preflight, `npm run smoke:ui` entrypoint, README usage, regression verification.
- Functionality: 9/10. UI smoke now checks FastAPI, React, and Chrome/Edge CDP before mutating verification fixtures and prints the missing service plus setup action.
- UI/design: 9/10. No product UI changes were needed; the browser smoke still validates the visible board, automation, live-write, and run-history flows.
- Security/privacy: 9/10. Preflight runs before fixture writes, so missing-service failures do not create temporary profiles or RAG knowledge rows.
- Performance: 9/10. Preflight uses short checks and normal API calls keep a longer timeout for real verification work.
- Tests: 9/10. Source verification passed compile, FastAPI checks, build, intentional missing-CDP preflight failure, direct CDP smoke, and `npm run smoke:ui`.
- Docs: 9/10. README now documents `smoke:ui`, `API_BASE`, `APP_URL`, `CDP_PORT`, and the preflight behavior.
- Next risks: UI smoke should not be run in parallel with `npm run verify:fastapi` because both interact with local dev server ports; a future pass should add a single orchestration command that runs them sequentially.

Verification:

- `python -m py_compile backend\app\main.py backend\app\services.py`: pass
- `npm run verify:fastapi`: pass
- `npm --prefix frontend run build`: pass
- `CDP_PORT=65534 node scripts\verify-ui-cdp.mjs`: expected preflight failure with missing Chrome CDP message
- `node scripts\verify-ui-cdp.mjs`: pass
- `npm run smoke:ui`: pass

Review notes:

- Review round 1 checked preflight runs before login, profile fixture creation, and temporary knowledge writes.
- Review round 2 checked the missing-CDP path produces a concrete service/action message and exits non-zero without the prior Windows assertion.
- Review round 3 checked the new npm script is documented and works when executed sequentially after server-oriented verification.
