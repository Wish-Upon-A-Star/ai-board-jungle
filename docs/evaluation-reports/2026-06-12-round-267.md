# 2026-06-12 Round 267 Evaluation

## Focus
- Align external access documentation with the new in-app admin Public Base URL workflow.

## Changes
- Updated README external access instructions to document:
  - `AI 기본값 > 외부 접속 도메인`,
  - checking `현재 접속` versus `OAuth 기준`,
  - using `현재 주소로 채우기`,
  - saving with `외부 도메인 저장`,
  - copying provider callback URLs from `프로필 > 진단`.
- Documented that the admin value is stored in `system_settings.public_base_url`.
- Documented that the persisted admin public URL takes precedence over provider-specific `AI_BOARD_*_OAUTH_REDIRECT_URI` values.
- Strengthened `verify:network-config` so this admin UI workflow remains documented.

## Verification
- `npm run verify:network-config` passed in the push clone.
- `npm run verify:readme` passed in the push clone.
- `npm run verify:text` passed in the push clone.
- Live folder README and `verify-network-config` script were synchronized.
- Live `npm run verify:network-config` passed.
- `npm run verify:readiness:compact` passed in the push clone with latest report 266 before this report.
- `npm run verify:command-scope` passed in the push clone.

## Scores
- Functionality: 9.2/10
- UI/design: 8.7/10
- Security/privacy: 8.2/10
- Performance: 8.0/10
- Tests: 9.2/10
- Docs: 8.8/10

## Current Result
- Operators and team members no longer have to infer from environment-variable docs only. The README now matches the actual app workflow for fixing OAuth callback base URL drift.
- The network verification gate now fails if the admin Public Base URL workflow disappears from the README.

## Next Risks
- The current public URL is still a temporary trycloudflare domain. A named Cloudflare Tunnel or custom domain is still required for durable OAuth registrations.
- Provider callback registration is still an external console task.
