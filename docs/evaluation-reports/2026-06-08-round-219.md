# Round 219 Evaluation

## Previous Reports Used

- Round 218: User-owned MCP auth metadata is stored on integration profiles, but the UI still needed MCP-first templates and a less crowded workflow layout.

## Scope

- Added MCP-first automation templates for GitHub latest commits to Notion and Notion changes to GitHub issues.
- Added template routing that sends users directly to MCP profile setup when required GitHub or Notion MCP profiles are missing.
- Split the main workflow into tabs: Automation, MCP / Profiles, Defaults, RAG, Board, and API.
- Added explicit GitHub/Notion MCP profile setup buttons in the profile tab.
- Updated the browser playtest to operate through the new tabbed UI.

## Scores

- Functionality: 9/10
- UI/design: 9/10
- Security/privacy: 10/10
- Performance: 9/10
- Tests: 10/10
- Docs: 9/10

## Review Rounds

1. Inspected the existing all-in-one main column and confirmed every major panel was rendered at once.
2. Added two MCP automation presets with user-owned MCP auth expectations and Korean output instructions.
3. Added frontend logic to detect missing MCP GitHub/Notion profiles and route directly to profile setup.
4. Added tab state and tab buttons for Automation, MCP / Profiles, Defaults, RAG, Board, and API.
5. Hid inactive panels with a stable `tab-hidden` class instead of unmounting them, reducing regression risk.
6. Added MCP profile setup buttons and prefilled profile defaults for GitHub and Notion.
7. Updated template verification to include the new MCP built-in presets.
8. Updated Playwright user-flow tests to click through the new tab layout.
9. Ran a focused MCP-template browser smoke proving a missing profile routes to the MCP profile form.
10. Re-ran frontend build and backend tests after the UI and test changes.

## Issues Found And Resolved

- Issue: The new MCP preset keys failed the existing template verifier.
  Resolution: Updated the verifier's documented preset list and required buttons.
- Issue: MCP connection templates did not include placeholders, which violated existing preset rules.
  Resolution: Added `{last_run}`, `{commit_list}`, `{summary_table}`, `{notion_changes}`, and related placeholders.
- Issue: The playtest clicked the new top `MCP / Profiles` tab instead of the API console MCP button.
  Resolution: Scoped API console clicks to `.api-buttons`.

## Verification

- Pass: `npm --prefix frontend run build`.
- Pass: `$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api.py -q` with 18 tests passing.
- Pass: `npm run verify:template-presets`.
- Pass: `$env:AI_BOARD_PLAYTEST_URL='http://127.0.0.1:3151'; npm run playtest:user`.
- Pass: focused browser smoke for missing MCP profile routing to `#integration-profiles`.

## Live Server Application Status

- PostgreSQL is installed and running.
- Live API is running on `0.0.0.0:8000` against PostgreSQL.
- Live web is running on `0.0.0.0:3000`.

## Next Risks

- Windows firewall still needs an administrator-created inbound rule for external access if another device cannot reach ports 3000 and 8000.
- Real MCP/API execution still requires user-owned GitHub and Notion credentials in the new MCP profile forms.
