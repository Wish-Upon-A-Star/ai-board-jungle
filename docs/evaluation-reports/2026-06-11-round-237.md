# AI Board Evaluation Report - Round 237

## Focus
- Users still needed clearer in-app guidance for where to enter provider API keys and what each field means.
- Added a service-aware manual profile guide directly above the manual profile form.

## Scores
- Functionality: 9.1/10 - Profile setup now gives provider-specific Base URL, token name, token permission, and verification guidance.
- UI/Design: 8.2/10 - The profile tab now explains the active provider without pushing all details into a generic help paragraph.
- Security/Privacy: 9.0/10 - The token field explicitly states values are encrypted and not shown again; no token values are rendered.
- Performance: 8.3/10 - Static client-side guide only; no backend or runtime cost.
- Tests: 9.1/10 - Build, user-token UX, text integrity, and a direct browser render check passed.
- Docs: 8.2/10 - This report records the exact UI behavior and verification evidence.
- Next Risks: 6.2/10 - Real external writes still need valid provider credentials, but users now have clearer steps to fix them.

## Verification
- `npm --prefix frontend run build` - pass.
- `npm run verify:user-token-ux` - pass against live app/API.
- `npm run verify:text` - pass.
- Live folder build - pass after syncing `App.jsx` and `style.css`.
- Live folder `npm run verify:user-token-ux` - pass.
- Browser smoke against live app:
  - Created a new user via API.
  - Opened the Profile tab.
  - Selected `figma` in the manual profile form.
  - Verified `.manual-profile-guide` contains `Figma 수동 연결`, `FIGMA_TOKEN`, and a Figma file URL example.
  - Screenshot saved to `ai-board/output/manual-profile-guide.png`.

## Review Rounds
1. Re-read Round 236 and identified the next gap: provider credential fixes were clearer in CLI diagnostics than in the product UI.
2. Inspected the Profile tab JSX and existing setup guide.
3. Confirmed the manual form had generic Base URL/token hints regardless of selected provider.
4. Added `manualProfileGuides` for GitHub, Notion, Figma, Google Calendar, and Custom/OpenAI API keys.
5. Wired the active guide to `integrationForm.source_kind`.
6. Added an accessible guide panel above the manual profile form.
7. Updated Base URL and token-name field hints to match the selected provider.
8. Added responsive CSS so the guide stays readable on mobile.
9. Ran existing browser token UX verification to ensure profile creation still works.
10. Ran a direct browser render smoke to confirm the guide changes when a provider is selected.

## Result
- Users no longer need to guess which field receives an AI/provider API key.
- Selecting a service in the manual form now immediately explains:
  - the expected Base URL value,
  - the exact token key name,
  - the minimum permission requirement,
  - and how to verify/fix common failures.
