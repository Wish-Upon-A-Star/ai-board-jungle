# 2026-06-12 Round 258 Evaluation

## Scope
- Extended OAuth redirect regression coverage from Figma to Google Calendar.
- Verified Figma and Google Calendar login button navigation against the public Cloudflare URL.
- Probed Google OAuth provider behavior to distinguish server redirect bugs from provider allowlist configuration.

## Scores
- Functionality: 8.7/10
- UI/design: 7.7/10
- Security/privacy: 8.3/10
- Performance: 8.0/10
- Tests: 8.6/10
- Docs: 7.9/10

## Verification
- `PYTHONPATH=backend python -m pytest backend/tests/test_api.py -k "oauth_start" -q` passed.
- `npm run verify:google-oauth-ui` passed against `https://railway-mediterranean-snap-populations.trycloudflare.com`.
- `npm run verify:figma-oauth-ui` passed against `https://railway-mediterranean-snap-populations.trycloudflare.com`.
- Direct Google authorize URL probe confirmed the app sends `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/google_calendar/callback`.
- `npm run verify:text` passed.
- `npm run build` passed.

## Result
- Added a backend regression test proving Google Calendar OAuth prefers `AI_BOARD_GOOGLE_OAUTH_REDIRECT_URI` over stale tunnel or request-host values.
- Figma remains valid: provider probe redirects to Figma login instead of an invalid redirect page.
- Google Calendar server output is now verified, but Google currently redirects to `redirect_uri_mismatch`; this means the Google Cloud OAuth client allowlist still needs the exact callback registered.

## Required External Console Value
- Google Cloud Console authorized redirect URI: `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/google_calendar/callback`

## Next Risks
- Temporary Cloudflare tunnel URLs still make OAuth providers fragile. A stable Cloudflare domain would remove repeated callback re-registration.
- The app should surface provider-side callback mismatch diagnostics more explicitly in the UI instead of relying on manual probe logs.
