# AI Board Evaluation Report - Round 246

## Scope
- Fixed and re-verified OAuth public redirect behavior after the Figma "Invalid redirect uri" report.
- Extended the same public-origin and labeled-credential guard to Google Calendar OAuth because it shares the same user-facing login pattern.
- Verified live public Cloudflare button clicks, not just backend URL generation.

## Scores
- Functionality: 8.5/10 - Figma and Google Calendar OAuth start flows now use the current public origin and sanitized client IDs. Full provider callback completion still depends on the provider console allowing the exact callback URL.
- UI/design: 7.5/10 - The profile login buttons are reachable and clickable; broader tab simplification and board readability remain separate work.
- Security/privacy: 8/10 - Client secrets are not exposed in logs, labeled desktop-token values are sanitized, and browser verification only prints masked client IDs.
- Performance: 8/10 - No runtime-heavy change; OAuth URL generation remains lightweight.
- Tests: 8.5/10 - Added backend regression coverage and live Playwright-style button verification for Google Calendar; Figma UI verification also passed again.
- Docs: 7/10 - This report records the required callback URLs; README still needs a compact end-user OAuth setup section.
- Next risks: 7/10 - Cloudflare quick-tunnel URL changes require updating Figma/Google provider callback allowlists or using a stable domain.

## Verification
- `python -m pytest backend/tests/test_api.py -k "figma_oauth_start or google_calendar_oauth_start"`: passed, 4 selected.
- `node --check scripts\verify-google-oauth-ui.mjs`: passed in live and push folders.
- `npm run verify:figma-oauth-ui`: passed on live and push folders.
- `npm run verify:google-oauth-ui`: passed on live and push folders.
- `npm run verify:text`: passed, 72 files checked.
- `npm run build`: passed.

## Review Round
- Checked the backend redirect-origin logic through existing Figma tests and the new Google Calendar test.
- Checked that Korean labels such as `클라이언트 ID:` and `클라이언트 보안 비밀번호:` are stripped before provider redirects.
- Checked live public UI button behavior for both Figma and Google Calendar.
- Checked that verification output masks client IDs and does not print secrets.
- Checked build and Korean text integrity after adding the verification script.

## Required Provider Callback URLs
- Figma: `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback`
- Google Calendar: `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/google_calendar/callback`

## Next Improvement
- Add a visible "OAuth 설정 진단" panel in the app that shows the exact current callback URLs and whether each provider is configured, so users do not have to infer provider console values.
