# AI Board Evaluation Report - Round 254

## Focus
Figma OAuth `Invalid redirect uri` remediation.

## What changed
- Live restart script now prefers the current Cloudflare tunnel URL from `.cloudflare-url.txt` over stale `AI_BOARD_PUBLIC_BASE_URL` values when starting OAuth-enabled servers.
- Local live `.env` public base URL was aligned to the current Cloudflare URL.
- Live servers were reapplied on ports 3000 and 8000.

## Verification
- `npm run apply:live` - passed.
- `npm run verify:figma-oauth` - passed; Figma authorize URL uses `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback`.
- `npm run verify:figma-oauth-ui` - passed; clicking the Figma login button opens Figma OAuth with the same redirect URI.
- `PYTHONPATH=backend python -m pytest backend/tests/test_api.py -q -k "figma_oauth or google_calendar_oauth_start_uses_external_origin or oauth_status"` - passed, 4 tests.
- `node --check scripts/apply-live.mjs` - passed.
- `npm run verify:evaluation-reports` - failed due to pre-existing report numbering gap: expected round 223 but found round 235.

## Scores
- Functionality: 8/10 - Figma OAuth URL generation and UI click path are correct; Figma developer app must still have the exact callback URL registered.
- UI/design: 7/10 - OAuth diagnostics expose the URL, but the setup flow can still be clearer for non-developers.
- Security/privacy: 7/10 - OAuth secrets remain server-side; restart script avoids stale redirect configuration. Local `.env` still contains secrets and must stay untracked.
- Performance: 8/10 - No runtime overhead beyond reading the tunnel URL at restart.
- Tests: 8/10 - API, UI, syntax, and live checks passed. Evaluation-report checker has an unrelated historical failure.
- Docs: 7/10 - This report records the exact required callback URL.

## Next risks
- Cloudflare quick tunnel domains can change; Figma requires exact redirect URI registration, so a changed tunnel URL will require updating the Figma developer app.
- A stable custom domain or named tunnel should replace random trycloudflare URLs for production-like use.
- Evaluation report numbering needs cleanup to restore the report verifier.
