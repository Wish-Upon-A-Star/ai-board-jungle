# 2026-06-12 Round 269 Evaluation

## Focus
- Fix webhook-based change detection for Notion events so new webhook payloads are not skipped as unchanged automation input.

## Changes
- Added a Notion webhook `external_event_key` based on target IDs and the raw webhook body.
- This event key now participates in the automation input hash, matching the GitHub webhook behavior.
- Repeated identical Notion webhook payloads still skip without AI/model/live-write work.
- Different Notion webhook payloads for the same target now run as changed input.
- GitHub and Notion webhook responses now include `runId` and `reason` for each triggered automation so callers can distinguish changed runs from skipped no-op checks.

## Verification
- `python -m pytest backend/tests/test_api.py -k "notion_webhook or github_webhook"` passed in the push clone.
- The repeated Notion webhook test now proves:
  - first payload creates a changed run,
  - duplicate payload skips with no run ID,
  - different payload creates another changed run,
  - GitHub issue live-write activity is created exactly twice.
- `npm run verify:text` passed in the push clone.
- Live server was restarted with `npm run apply:live`.
- Public health passed on `https://railway-mediterranean-snap-populations.trycloudflare.com/api/health`.
- Live `API_BASE=https://railway-mediterranean-snap-populations.trycloudflare.com npm run verify:contract` passed.
- `npm run verify:readiness:compact` passed in the push clone with latest report 268 before this report.

## Scores
- Functionality: 9.4/10
- UI/design: 8.7/10
- Security/privacy: 8.3/10
- Performance: 8.2/10
- Tests: 9.4/10
- Docs: 8.8/10

## Current Result
- GitHub and Notion webhook-triggered automation now use payload-specific change keys.
- The app better satisfies the requirement that AI/model calls and live writes happen only when watched webhook input changes.

## Next Risks
- Real provider webhook registration still must be completed in GitHub/Notion consoles.
- Live Notion webhook verification with a real Notion payload depends on the workspace integration and token permissions.
