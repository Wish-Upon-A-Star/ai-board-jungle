# Round 37 Evaluation - Deterministic Full Verification DB

- Scope: full verification DB reset, README docs, regression verification.
- Functionality: 9/10. `verify:full` and `verify:full:quick` now delete `data/full-verify.db` plus SQLite sidecar files before seeding, keeping each run deterministic.
- UI/design: 9/10. No product UI changes were needed; UI CDP smoke still verifies the real rendered workflow after a fresh seed.
- Security/privacy: 9/10. Resetting the verification DB prevents stale test users, tokens, profiles, and temporary RAG records from carrying across full verification runs.
- Performance: 9/10. DB reset is a tiny local file operation and does not slow the quick no-install path meaningfully.
- Tests: 10/10. Source `npm run verify:full:quick` passed after DB reset and a follow-up port check confirmed 3000/8000 were clear.
- Docs: 9/10. README now states that both full verification commands reset `data/full-verify.db` at startup.
- Next risks: `verify-fastapi.mjs` still reuses `data/fastapi-verify.db`; a future pass should apply the same deterministic DB reset there.

Verification:

- `npm run verify:full:quick`: pass
- `Get-NetTCPConnection -LocalPort 3000,8000 -State Listen`: no listeners after cleanup

Review notes:

- Review round 1 checked reset runs after stopping local servers and before compile/test/seed.
- Review round 2 checked reset covers SQLite `db`, `db-shm`, and `db-wal` files.
- Review round 3 checked README documents reset behavior for both full and quick full verification commands.
