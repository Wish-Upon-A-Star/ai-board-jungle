# 2026-06-11 Round 230 Evaluation

## Focus
- Fix the gap where OpenAI/audio transcription was treated as documentation instead of a usable product feature.
- Make the Knowledge tab expose real OpenAI transcription controls: user-owned API key profile, transcription model, and transcription prompt.
- Keep the live server applied, not only the git clone.

## Implemented
- Backend `POST /api/ai/transcribe` now accepts `integration_profile_id`.
- The backend validates that the selected profile belongs to the logged-in user, is OpenAI/OpenAI-compatible, and has a stored token.
- Transcription responses and activity logs now include the profile id/name used for the call.
- Knowledge tab now has an `OpenAI 음성 전사` settings block with:
  - OpenAI API key profile selection
  - transcription model selection (`gpt-4o-mini-transcribe`, `gpt-4o-transcribe`, `whisper-1`)
  - transcription prompt input
  - shortcut button to save an OpenAI key when no profile exists
- Browser transcription verification now creates a temporary OpenAI profile and asserts that profile id, selected model, and prompt are submitted.
- Live `ai-board` source was patched and the 8000 backend was restarted.
- Existing board readability CSS improvements remain in this round's frontend stylesheet diff.

## Verification
- `node --check scripts/verify-transcription-ui.mjs` - passed
- `npm --prefix frontend run build` in push clone - passed
- `PYTHONPATH=backend python -m pytest backend/tests/test_api.py -q` - passed, 51 tests
- `npm --prefix frontend run build` in live folder - passed
- `npm run verify:transcription-ui` after live backend restart - passed
- `npm run verify:user-token-ux` - passed
- `git diff --check` - passed
- Live health: `http://127.0.0.1:8000/api/health` - ok, PostgreSQL active
- Live frontend: `http://127.0.0.1:3000` - HTTP 200
- Live OpenAPI schema now includes `integration_profile_id` for `/api/ai/transcribe`.

## Scores
- Functionality: 9.1/10 - Audio transcription now uses selected user-owned OpenAI profile settings instead of a hidden fixed model path.
- UI/Design: 8.2/10 - Knowledge tab exposes the transcription controls in-context with a direct path to save an OpenAI key.
- Security/Privacy: 8.6/10 - Profile ownership and token presence are validated server-side; raw API keys remain redacted.
- Performance: 8.0/10 - Existing direct upload and ffmpeg chunking path remains; no extra AI call is made until the user uploads audio.
- Tests: 9.0/10 - Backend and browser tests now cover profile id, model, prompt, and transcript form fill.
- Docs: 7.8/10 - README was not changed in this round because the issue was implementation, not docs.
- Next Risks: 7.5/10 - A future pass should add a real OpenAI sandbox/integration test when a test key is intentionally available.

## Review Notes
- Round 1: Found the live backend had not reloaded the new endpoint signature; restarted only the 8000 uvicorn process and rechecked OpenAPI.
- Round 2: Found the existing browser transcription test did not prove model/profile submission; updated it to assert multipart payload content.
- Round 3: Checked user API-key persistence flow after the transcription UI change; no regression found.
