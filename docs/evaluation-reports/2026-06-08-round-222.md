# Round 222 Evaluation

## Scope

- Fixed the MCP login button UX when OAuth provider apps are not configured.
- Changed `/api/oauth/{provider}/start` to return setup-card data instead of a plain 400 error when client id/secret are missing.
- Added visible OAuth setup cards with provider app link, callback URL, and exact server env variables.
- Fixed live apply script so Cloudflare users call same-origin `/api` instead of browser-local `127.0.0.1:8000`.
- Re-applied the live server and verified the Cloudflare UI.

## Scores

- Functionality: 10/10
- UI/design: 9/10
- Security/privacy: 9/10
- Performance: 9/10
- Tests: 10/10
- Docs: 10/10

## Review Rounds

1. Reproduced the user's complaint: clicking MCP login showed only an error, not a login or setup link.
2. Confirmed the server cannot build a real provider login URL without OAuth client id/secret.
3. Changed the backend missing-config response to return setup link, redirect URI, and required env names.
4. Changed the frontend to display a setup card instead of only a top error.
5. Found Cloudflare `Failed to fetch` after live apply because `VITE_API_BASE` was forced to `127.0.0.1:8000`.
6. Fixed `apply-live.mjs` to keep `VITE_API_BASE` empty by default so external users use same-origin proxying.
7. Rebuilt, retested, and re-applied the live server.
8. Browser-verified the Cloudflare URL: no `Failed to fetch`, OAuth setup link visible, callback URL and env values present.

## Verification

- Pass: `npm --prefix frontend run build`.
- Pass: `$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api.py -q` with 20 tests passing.
- Pass: `http://127.0.0.1:3000/api/health`.
- Pass: Cloudflare browser UI check:
  - `OAuth 앱 만들기` link points to `https://github.com/settings/developers`.
  - Callback value is `https://cheats-biggest-partial-decorating.trycloudflare.com/api/oauth/github/callback`.
  - Env value includes `AI_BOARD_GITHUB_OAUTH_CLIENT_ID` and `AI_BOARD_GITHUB_OAUTH_CLIENT_SECRET`.

## Live Server Status

- Live web: `0.0.0.0:3000`.
- Live API: `0.0.0.0:8000`.
- Cloudflare quick tunnel: `https://cheats-biggest-partial-decorating.trycloudflare.com`.
- PostgreSQL health: OK.

## Next Risks

- Real OAuth login still requires creating provider OAuth apps and setting the client IDs/secrets on the live server.
- Quick Tunnel URL is temporary; a named Cloudflare Tunnel is still needed for a stable domain.
