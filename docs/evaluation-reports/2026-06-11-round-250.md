# Evaluation Report - Round 250

## Scope
- Investigated the reported Figma `Invalid redirect uri` failure.
- Verified the live OAuth start API and UI button flow against the current Cloudflare public URL.
- Updated the live server's `.cloudflare-url.txt` from the stale `cheats-biggest-partial-decorating` tunnel to the active `railway-mediterranean-snap-populations` tunnel.

## Scores
- Functionality: 8.9/10 - The app now sends `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback` from both API and UI flows. Figma console allowlist still must contain the exact same URL.
- UI/design: 8.0/10 - The Profile tab OAuth diagnostics show the exact callback URLs and setup links, but the profile/connect settings are still dense.
- Security/privacy: 8.2/10 - OAuth client secret is not exposed; diagnostics only show callback, scope, provider status, and setup URLs.
- Performance: 8.4/10 - No additional runtime load was added.
- Tests: 8.7/10 - Figma OAuth API, Figma UI click, and OAuth diagnostics were re-run against the live public URL.
- Docs: 8.4/10 - README already documents provider callback registration; this report records the exact active Figma callback.
- Next risks: 7.0/10 - Cloudflare quick-tunnel URLs are temporary. If the tunnel URL changes again, Figma, Google, GitHub, and Notion OAuth callback allowlists must be updated again unless a stable domain/named tunnel is used.

## Verification
- `npm run apply:live`: passed; live API/web restarted on `0.0.0.0:8000` and `0.0.0.0:3000`.
- `npm run verify:figma-oauth`: passed against `https://railway-mediterranean-snap-populations.trycloudflare.com`.
- `npm run verify:figma-oauth-ui`: passed against the same public URL.
- `npm run verify:oauth-diagnostics-ui`: passed against the same public URL.
- Direct Figma authorize URL smoke check: reached Figma login page, not an immediate app-generated redirect rejection.

## Exact Figma Callback Required
`https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback`

## Review Rounds
1. Checked backend OAuth URL generation and callback origin selection.
2. Checked live API `/api/oauth/status` and `/api/oauth/figma/start` output with `x-ai-board-public-origin`.
3. Checked UI Figma login button request interception with Playwright.
4. Checked live `.cloudflare-url.txt` stale tunnel mismatch and corrected it.
5. Restarted live servers and re-ran the Figma/OAuth verification commands.

## Result
The AI Board side now consistently uses the current public Cloudflare URL for Figma OAuth. If Figma still shows `Invalid redirect uri`, the remaining required action is to add the exact callback above to the Figma Developer App's redirect URLs for the client ID shown in `Desktop/figma.txt`.
