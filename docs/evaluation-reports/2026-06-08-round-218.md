# Round 218 Evaluation

## Previous Reports Used

- Round 217: Notion automation writes now use a visible table block, but real app-driven Notion mutation still needs real user-owned credentials and a live backend apply after PostgreSQL is reachable.

## Scope

- Added user-owned MCP auth metadata to integration profiles: auth type, MCP server URL, authenticated subject, and scopes.
- Preserved raw credential privacy: API responses expose MCP metadata and token storage status, but not the secret value.
- Connected the frontend integration profile form to submit MCP auth metadata and display saved MCP identity/scope details.
- Documented that AI Board cannot reuse Codex desktop connector sessions; users must register their own MCP/API credentials in profiles.
- Verified the app flow on separate test ports without stopping the current live server.

## Scores

- Functionality: 9/10
- UI/design: 9/10
- Security/privacy: 10/10
- Performance: 9/10
- Tests: 10/10
- Docs: 9/10

## Review Rounds

1. Re-read Round 217 and the user's MCP clarification that app automations should authenticate through user-owned MCP credentials.
2. Inspected backend profile model, schema, serializer, and SQLite migration fallback for MCP fields.
3. Inspected frontend integration profile save flow and found MCP fields were not yet submitted from the UI.
4. Added frontend MCP auth type/server/user/scope controls and card display for saved MCP identity metadata.
5. Found a PowerShell file rewrite corrupted JSX strings; restored the frontend/README from the clean push clone and re-applied edits using targeted patches.
6. Re-ran frontend production build to verify JSX and layout compilation after the restore.
7. Re-ran backend API tests and confirmed MCP profile ownership/redaction coverage passes.
8. Started a separate API verification server on port 8019 and confirmed contract verification against the modified backend.
9. Started a separate frontend test server on port 3151 and ran the Playwright user-flow playtest through registration, profile save, automation create/run/share/delete, RAG collect, MCP console, and activity filters.
10. Re-checked live-server status: the existing live server remains up, but latest code still cannot be safely applied until PostgreSQL is reachable.

## Issues Found And Resolved

- Issue: MCP auth metadata existed in backend fields but the UI did not submit or show it.
  Resolution: Added UI controls for auth type, MCP server, MCP user, and MCP scopes; save now serializes scopes as an array.
- Issue: Raw MCP credential could be confused with Codex's own connector token.
  Resolution: README now states that Codex connector sessions are not available to the app and that AI Board stores user-owned credentials only.
- Issue: A manual PowerShell rewrite corrupted non-ASCII JSX strings.
  Resolution: Restored affected files from the clean push clone and re-applied only small targeted patches; frontend build now passes.

## Verification

- Pass: `npm --prefix frontend run build`.
- Pass: `$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api.py -q` with 18 tests passing.
- Pass: `$env:API_BASE='http://127.0.0.1:8019'; npm run verify:contract`.
- Pass: `$env:AI_BOARD_PLAYTEST_URL='http://127.0.0.1:3151'; npm run playtest:user`.

## Live Server Application Status

- The current live server was not stopped during this round.
- The latest backend still cannot be promoted to live with `npm run apply:live` until PostgreSQL is installed, started, or `AI_BOARD_DATABASE_URL` points to a reachable PostgreSQL instance.
- The app automation can now store MCP profile auth metadata, but real GitHub/Notion writes still require real user-owned credentials instead of placeholder desktop token files.

## Next Risks

- Install/start PostgreSQL and run `npm run apply:live` so the live server uses the latest MCP/profile/webhook code.
- Replace desktop token placeholders with real user-owned GitHub, Notion, and webhook secret values.
- Run `npm run demo:github-notion-webhook` after live apply and page sharing to prove app automation writes to the small Notion page without manual Codex intervention.
