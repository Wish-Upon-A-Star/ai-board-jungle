# 2026-06-12 Round 268 Evaluation

## Focus
- Strengthen live API contract coverage for the admin Public Base URL and OAuth callback generation.

## Changes
- Extended `scripts/verify-api-contract.mjs` to:
  - read and preserve the current `systemSettings.publicBaseUrl`,
  - save a contract test public URL through `PUT /api/system/settings`,
  - verify trailing slash trimming and database source,
  - verify `/api/oauth/status` ignores a temporary request origin when a saved public URL exists,
  - verify every provider redirect URI uses the saved public URL,
  - restore the original public URL even on cleanup.
- Added `system_settings_public_oauth_origin` to the contract verifier's checked list.

## Verification
- `node --check scripts/verify-api-contract.mjs` passed.
- Push-clone `API_BASE=https://railway-mediterranean-snap-populations.trycloudflare.com npm run verify:contract` passed.
- Live-folder `API_BASE=https://railway-mediterranean-snap-populations.trycloudflare.com npm run verify:contract` passed.
- `npm run verify:text` passed in the push clone.
- `npm run verify:readiness:compact` passed in the push clone with latest report 267 before this report.

## Scores
- Functionality: 9.3/10
- UI/design: 8.7/10
- Security/privacy: 8.3/10
- Performance: 8.0/10
- Tests: 9.3/10
- Docs: 8.8/10

## Current Result
- The same live contract verifier that checks profiles, automation runs, activity logs, MCP, redaction, and custom connection validation now also proves that saved admin Public Base URL settings drive OAuth provider callback URLs.
- The verifier restores the previous setting after the check, so it can run against the live demo without leaving a fake domain configured.

## Next Risks
- Provider consoles still require manual registration of the displayed callback URLs.
- A stable custom domain or named Cloudflare Tunnel remains needed to remove trycloudflare URL churn entirely.
