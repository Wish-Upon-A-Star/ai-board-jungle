# 2026-06-11 Round 231 Evaluation

## Focus
- Fix the board/share separation problem behind the user's complaint that board posts and shared automations did not read like separate concepts.
- Add browser-level UI verification for board readability across desktop and mobile.

## Implemented
- Changed backend post classification so a normal board post is not moved to the shared automation tab merely because moderation/RAG tags include `automation`.
- Shared automation posts are now identified by `automation_task_id` or `[자동화]` title prefix.
- Added API regression assertions proving:
  - normal posts remain in `kind=board`
  - shared automation posts appear in `kind=automation`
  - shared automation posts do not appear in `kind=board`
- Added `scripts/verify-board-readable-ui.mjs`.
- Added `npm run verify:board-ui`.
- The board UI verifier creates a temporary normal post and shared automation, checks separated tabs, checks no horizontal overflow on desktop/mobile, checks title overflow, checks the apply-shared-automation button, captures screenshots, and cleans up fixtures.
- Added `output/` to `.gitignore` so Playwright screenshots do not pollute commits.
- Patched the live app CSS for the currently running 3000 server so card radius and overflow behavior match the app UI rules.

## Verification
- `npm run verify:board-ui` - passed
- `PYTHONPATH=backend python -m pytest backend/tests/test_api.py -q` - passed, 51 tests
- `npm --prefix frontend run build` - passed
- `git diff --check` - passed
- Live backend restarted after service classification change and health remained OK with PostgreSQL.
- Screenshots generated locally:
  - `output/playwright/board-readable-desktop-posts.png`
  - `output/playwright/board-readable-desktop-shares.png`
  - `output/playwright/board-readable-mobile-shares.png`

## Scores
- Functionality: 9.2/10 - Board and shared automation are now separated by durable backend identity instead of fragile moderation tags.
- UI/Design: 8.6/10 - Desktop/mobile readability is now covered by automated visual-flow checks.
- Security/Privacy: 8.5/10 - Verification uses admin demo fixtures and cleans them up; no tokens are exposed.
- Performance: 8.2/10 - The verifier checks rendered overflow without adding runtime overhead to the app.
- Tests: 9.2/10 - API classification and browser UI readability are both covered.
- Docs: 8.0/10 - Evaluation report captures the behavior and screenshots; README unchanged because this was implementation and QA work.
- Next Risks: 7.6/10 - Some older Korean mojibake strings remain in legacy helper text and should be cleaned in a later text-integrity pass.

## Review Notes
- Round 1: Browser verifier failed because a normal post was classified as automation due moderation-suggested tags.
- Round 2: Backend classification was corrected and API regression assertions were added.
- Round 3: Browser verifier then found live card radius exceeded 8px; live CSS was patched and verifier passed.
