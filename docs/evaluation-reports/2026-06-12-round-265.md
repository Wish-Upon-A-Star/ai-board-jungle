# 2026-06-12 Round 265 Evaluation

## Focus
- Fix the Figma `Invalid redirect uri` path by making the live app's externally registered callback URL controllable from the admin UI.

## Changes
- Added a persisted `system_settings` table for server-level configuration.
- Added `GET /api/system/settings` and admin-only `PUT /api/system/settings`.
- Changed OAuth callback generation so a saved admin `public_base_url` overrides stale provider-specific redirect URI environment values.
- Added an admin "Public Base URL" card in the AI defaults/settings tab.
- Added `verify:system-settings-ui` to save the public URL through the live UI and verify Figma OAuth uses that URL.
- Applied the live server setting to `https://railway-mediterranean-snap-populations.trycloudflare.com`.

## Verification
- `python -m pytest backend/tests/test_api.py -k "system_public_base_url or figma_oauth_start_prefers_registered_redirect_override or figma_oauth_start_uses_external_host"` passed in the push clone.
- `npm run build` passed in the push clone.
- `node --check scripts/verify-system-settings-ui.mjs` passed.
- Live server was restarted with `npm run apply:live`.
- Public health passed on `https://railway-mediterranean-snap-populations.trycloudflare.com/api/health`.
- `npm run verify:contract` passed against the live server.
- `npm run verify:system-settings-ui` passed and confirmed Figma callback `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback`.
- `npm run verify:figma-oauth-ui` passed and confirmed the Figma login button sends the same `redirect_uri`.
- `npm run verify:oauth-diagnostics-ui` passed.
- `npm run verify:readiness:compact`, `npm run verify:text`, `npm run verify:command-scope`, and `npm run verify:frontend-helpers` passed in the push clone.

## Scores
- Functionality: 9.2/10
- UI/design: 8.5/10
- Security/privacy: 8.2/10
- Performance: 8.0/10
- Tests: 9.1/10
- Docs: 8.5/10

## Current Result
- The live app now generates Figma OAuth callback URLs from the saved admin public URL instead of a stale redirect override.
- The correct URL to register in Figma is now visible and copyable in the app: `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback`.
- If the Cloudflare URL changes, the admin can update the public URL in the app without editing server files.

## Next Risks
- The current Cloudflare URL is still a temporary trycloudflare hostname, so Figma OAuth will break again if the tunnel URL rotates.
- A named Cloudflare Tunnel or custom domain is still the durable fix for users outside the local network.
- Provider developer consoles still require the exact callback URL to be registered manually.
