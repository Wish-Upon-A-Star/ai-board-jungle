# Round 209 Evaluation

## Previous Reports Used

- Round 208: GitHub collector, webhook matching, and live issue writing now share one repository parser. The next code-side risk was real user URL variance beyond SSH and plain `github.com` URLs.

## Scope

- Normalized `www.github.com` repository URLs in the shared GitHub parser.
- Added parser coverage for `https://www.github.com/owner/repo` URLs.
- Verified that the shared parser change protects collector, webhook matching, and live GitHub issue writing without adding another parser path.

## Scores

- Functionality: 10/10
- UI/design: 9/10
- Security/privacy: 10/10
- Performance: 9/10
- Tests: 10/10
- Docs: 10/10

## Review Rounds

1. Re-read Round 208 and selected user-entered GitHub URL variance as the next consistency risk.
2. Inspected `parse_github_repo()` and found it rejected `www.github.com` URLs.
3. Confirmed the same parser is used by collectors, GitHub webhook matching, and live issue writing.
4. Added host normalization that strips a leading `www.` before comparing to `github.com`.
5. Added direct test coverage for `https://www.github.com/acme/private-repo`.
6. Re-ran all backend tests.
7. Re-ran backend syntax compilation for collector, writer, main app, and tests.
8. Re-ran text-output verification.
9. Re-ran evaluation-report and readiness-output verification.
10. Re-ran PostgreSQL verification to keep the environment blocker explicit.

## Issues Found And Resolved

- Issue: A user could paste `https://www.github.com/owner/repo`, causing GitHub RAG collection, webhook matching, or issue-writing dry-run setup to reject an otherwise valid GitHub repository.
  Resolution: The shared GitHub repository parser now normalizes `www.github.com` to `github.com`.

## Verification

- Pass: `PYTHONPATH=backend python -m pytest backend/tests` with 13 tests passing.
- Pass: `PYTHONPATH=backend python -m py_compile backend/app/collectors.py backend/app/live_writers.py backend/app/main.py backend/tests/test_api.py`.
- Pass: `npm run verify:text-output`.
- Pending environment: `npm run verify:postgres` still fails because PostgreSQL is not installed or listening at `localhost:5432`.

## Next Risks

- PostgreSQL remains the only active runtime blocker in this environment. Runtime verification cannot pass until a PostgreSQL server is available at `postgresql://ai_board:ai_board@localhost:5432/ai_board` or `AI_BOARD_DATABASE_URL` points to a reachable PostgreSQL database.
- After PostgreSQL is available, rerun PostgreSQL, FastAPI, full browser, webhook, and automation smoke checks against the PostgreSQL engine.
