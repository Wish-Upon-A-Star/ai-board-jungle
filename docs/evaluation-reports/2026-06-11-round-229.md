# AI Board Evaluation Report - 2026-06-11 Round 229

## Scope
- Added a browser-level non-admin user verification for AI API key and integration-token UX.
- Verified a real user can register, save an OpenAI API key profile, save GitHub and Notion token profiles, create an automation using the selected user profile, log out, log back in, and still see the encrypted OpenAI profile.
- Added cleanup so repeated verification does not leave test automations or profiles behind.

## Verification
- `npm run verify:user-token-ux` - passed
- `PYTHONPATH=backend python -m pytest backend/tests/test_api.py -q` - passed, 51 tests
- `npm run verify:transcription-ui` - passed
- `npm --prefix frontend run build` - passed
- `git diff --check` - passed
- Live API health `http://127.0.0.1:8000/api/health` - passed, PostgreSQL active
- Live frontend `http://127.0.0.1:3000/` - HTTP 200

## Scores
- Functionality: 9.0/10 - The highest-friction non-admin token flow now has browser coverage across OpenAI, GitHub, Notion, automation selection, and relogin persistence.
- UI/Design: 7.9/10 - The flow is usable from the `프로필` tab, but the wording mismatch from earlier expectations (`계정 연결` vs `프로필`) should still be considered in the next UI simplification pass.
- Security/Privacy: 8.7/10 - Raw API keys are entered through password inputs, API responses are checked for redaction, and relogin confirms encrypted persisted storage.
- Performance: 8.3/10 - Browser smoke is fast and cleans up created automations/profiles after verification.
- Tests: 9.0/10 - Added a repeatable end-user browser smoke for a previously user-reported confusion point.
- Docs: 7.7/10 - Evaluation report added; README was not changed because this was behavior and verification work.
- Next Risks: 6.9/10 - Remaining risks are real-token live demo page writes and a broader visual polish/playtest pass for board readability and tab-level information density.

## Findings And Fixes
- Found: the first script attempt used the old phrase `계정 연결`; the actual current UI tab is `프로필`.
- Fixed: the verifier now targets the current `프로필` tab and uses selectors for brittle relogin/actions.
- Found: logout left the auth form in register mode, so relogin could fill the wrong field.
- Fixed: verifier explicitly switches back to login mode before relogin.
- Verified: generated OpenAI/GitHub/Notion token strings do not appear in profile API responses, automation stores the selected GitHub profile ID, and OpenAI profile persists with `tokenStorage: encrypted`.
