# Round 35 Evaluation - Sequential Full Verification

- Scope: managed `verify:full` orchestration, HTTP smoke + UI CDP smoke sequencing, README docs, port cleanup review.
- Functionality: 9/10. `npm run verify:full` now runs compile, dependency checks, backend tests, frontend build, seed, HTTP smoke, and UI CDP smoke under one managed server lifecycle.
- UI/design: 9/10. No product UI changes were needed; the orchestrated UI smoke still verifies the visible board, automation, RAG, live-write, and run-history flows.
- Security/privacy: 9/10. Full verification keeps the CDP fixture cleanup behavior and uses a dedicated SQLite DB at `./data/full-verify.db`.
- Performance: 8/10. The command is intentionally heavier than targeted checks, but avoids parallel port contention and repeated manual setup.
- Tests: 10/10. Source `npm run verify:full` passed end-to-end, including HTTP smoke and UI CDP smoke; a follow-up port check confirmed 3000/8000 were clear.
- Docs: 9/10. README documents `verify:full` and warns against running `verify:fastapi` and `smoke:ui` in parallel.
- Next risks: `verify:full` still installs Python/frontend dependencies every run; a future pass should add a faster no-install variant for tight iteration.

Verification:

- `npm run verify:full`: pass
- `Get-NetTCPConnection -LocalPort 3000,8000 -State Listen`: no listeners after cleanup

Review notes:

- Review round 1 checked the orchestrator starts FastAPI and React once, then runs HTTP smoke before UI CDP smoke.
- Review round 2 found server processes could remain when started through shell wrappers; fixed by spawning long-running servers directly and using Vite's JS entry from the `frontend` cwd.
- Review round 3 checked final cleanup repeats long enough for late child startup races and verified ports were clear after completion.
