# Round 50 Evaluation - API Contract Smoke

- Scope: API contract verification for the React automation workflow, integration profiles, token redaction, activity logs, scheduler, and MCP.
- Functionality: 10/10. Added `npm run verify:contract` and wired it into `verify:full` and `verify:full:quick` after managed FastAPI/React startup.
- UI/design: 9/10. No visual UI changed; the contract check protects the API response shapes consumed by the automation, integration profile, activity, and run-history UI.
- Security/privacy: 10/10. The contract check verifies integration profile responses do not expose raw token fields and uses tokenless/dry-run paths only.
- Performance: 9/10. The check is lightweight HTTP smoke and creates only temporary profile/task records that are cleaned up in `finally`.
- Tests: 10/10. Source `npm run verify:full:quick` passed after fixing over-strict assumptions about immediate run and activity response shapes.
- Docs: 10/10. README and `docs/submission-checklist.md` now explain `verify:contract`, and README verification enforces the checklist reference.
- Next risks: A future pass should clean up mojibake Korean strings in smoke fixtures and backend default messages so validation output and demo seed text are readable.

Verification:
- `npm run verify:readme`: pass, checklist and screenshot validation still pass.
- `npm run verify:full:quick`: pass, including hygiene, README, backend tests, frontend build, API contract smoke, HTTP smoke, UI CDP smoke, and MCP smoke.
- `scripts/verify-api-contract.mjs`: exercised profile settings, provider readiness, integration profile create/list/collect/dry-write, automation create/run/history/tick, integration activities, MCP RPC, and token redaction.
- `Get-NetTCPConnection -LocalPort 3000,3001,8000,8788 -State Listen`: no listeners after cleanup.

Review notes:
- Review round 1 found and fixed an over-strict task contract that expected `runCount`/`lastRun`; the actual UI contract uses `runs`, `lastResult`, and `lastRunAt`.
- Review round 2 found and fixed an over-strict immediate run contract; `/run` returns a compact run object while `/runs` returns full history rows.
- Review round 3 found and fixed activity dry-run validation; dry-run status is stored in `activity.details.dryRun`, not a top-level field.
