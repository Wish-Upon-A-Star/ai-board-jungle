# 2026-06-12 Round 262 Evaluation

## Focus
- Reduce the risk that a stale Cloudflare quick tunnel URL breaks OAuth callbacks after a stable domain is configured.

## Changes
- Updated `scripts/apply-live.mjs` so explicit `AI_BOARD_PUBLIC_BASE_URL` takes precedence over `.cloudflare-url.txt`.
- Updated `scripts/verify-network-config.mjs` to catch regressions in that precedence rule.
- Documented that fixed public origin overrides the previous quick tunnel file during live restart.

## Verification
- `npm run verify:network-config` passed in the push clone.
- `npm run verify:readme` passed in the push clone.
- `npm run verify:readme-output` passed in the push clone.
- `npm run verify:network-config` passed in the live folder.
- `npm run verify:readiness:compact` passed in the live folder, 13/13.
- Public health passed on `https://railway-mediterranean-snap-populations.trycloudflare.com/api/health`.

## Scores
- Functionality: 8.8/10
- UI/design: 8.1/10
- Security/privacy: 8.0/10
- Performance: 8.0/10
- Tests: 8.7/10
- Docs: 8.5/10

## Current Result
- When a stable domain is configured with `AI_BOARD_PUBLIC_BASE_URL`, live restart uses that domain for Figma and Google OAuth callback overrides even if an older quick tunnel URL file remains.
- This makes named Cloudflare tunnel migration safer and avoids silently reusing a stale temporary callback URL.

## Next Risks
- The actual production-grade stable domain still needs the Cloudflare account tunnel UUID and DNS route configured outside the app.
- Provider consoles still need the exact stable callback URLs registered once the stable domain is chosen.
- The UI should eventually expose a guided “stable domain checklist” instead of relying only on README/operator scripts.
