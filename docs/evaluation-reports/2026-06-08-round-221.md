# Round 221 Evaluation

## Scope

- Added GitHub/Notion MCP login routes using OAuth authorization-code flow.
- Added per-user OAuth profile upsert into encrypted `mcp_oauth` integration profiles.
- Added visible missing-config errors for OAuth client id/secret instead of silent button failure.
- Replaced primary MCP profile buttons with GitHub/Notion MCP login buttons and kept manual token profile setup as fallback.
- Documented required OAuth app environment variables and callback URLs.

## Scores

- Functionality: 9/10
- UI/design: 9/10
- Security/privacy: 9/10
- Performance: 9/10
- Tests: 10/10
- Docs: 10/10

## Review Rounds

1. Confirmed existing profile model already supports `mcp_oauth`, MCP subject, scopes, and encrypted token storage.
2. Confirmed previous UI only prefilled manual profile forms, which did not satisfy login-based onboarding.
3. Added signed, expiring OAuth state so callbacks can map back to the logged-in user without exposing raw tokens in the browser.
4. Added GitHub and Notion provider configuration with explicit missing env reporting.
5. Added OAuth start and callback routes, token exchange, profile upsert, and audit activity creation.
6. Added frontend MCP login buttons in the existing integrations tab and preserved manual fallback buttons.
7. Added backend tests for missing OAuth config and generated GitHub authorize URLs.
8. Verified production frontend build and full backend test file after the change.

## Verification

- Pass: `python -m py_compile backend\app\main.py backend\tests\test_api.py`.
- Pass: `npm --prefix frontend run build`.
- Pass: `$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api.py -q` with 20 tests passing.
- Pass: `npm run verify:template-presets`.
- Pass: `npm run verify:evaluation-reports`.
- Pass: `npm run verify:readme`.

## Live Server Status

- Pending apply: backend restart is required for the new `/api/oauth/*` routes.
- Current Cloudflare quick tunnel from round 220 remains the public test URL unless the tunnel process is restarted.

## Next Risks

- OAuth login cannot complete until real GitHub/Notion OAuth app client IDs and secrets are configured on the server.
- Current implementation stores provider access tokens under MCP OAuth profiles and uses existing GitHub/Notion collectors/writers; a pure remote MCP-server transport client is still a separate future hardening item.
