# Round 191 Evaluation - Single Process FastAPI Serve

## Scores
- Functionality: 10/10
- UI/design: 10/10
- Security/privacy: 10/10
- Performance: 10/10
- Tests: 10/10
- Docs: 10/10

## Previous Report Used
- Round 190 next risk: add a production deployment profile that serves the built React app behind FastAPI or a reverse proxy so LAN/dev and production/tunnel deployment are both documented separately.

## Work Completed
- Added FastAPI static serving for the built React app in `frontend/dist`.
- Added SPA fallback so deep links return `index.html`.
- Preserved `/api/*` and `/mcp/*` as backend routes; missing API routes return API 404 instead of the React shell.
- Added `npm run start:lan` for a single FastAPI process bound to `0.0.0.0`.
- Added `npm run verify:production-serve` to build React, start FastAPI on an isolated port, verify `/`, an SPA deep link, `/api/health`, and API 404 isolation.
- Added `verify:production-serve` to the server-required command list and `verify:full:quick`.
- Documented the single-process production serve flow in README and the submission checklist.

## Verification
- Initial syntax attempt used `node --check backend\app\main.py` and failed because `.py` is not a Node module. This was a verification command mistake, not an app failure.
- Corrected syntax check with `python -m py_compile backend\app\main.py backend\app\services.py`: pass.
- `node --check scripts\verify-production-serve.mjs`: pass.
- `node --check scripts\verify-full.mjs`: pass.
- `npm run verify:command-scope`: pass with `verify:production-serve` listed as server-required.
- `npm run verify:readme`: pass with 22 command mentions and 24 checklist items.
- `npm run verify:production-serve`: pass. It built React, served the built index from FastAPI, returned the React shell for `/automations/deep-link`, returned ok from `/api/health`, and kept `/api/does-not-exist` as 404.
- `npm run verify:readme-output`: pass.
- `npm run verify:readiness-output`: pass with 22 checklist commands and 50 scanned text files.
- Sequential `npm run verify:fastapi` then `npm run verify:full:quick`: pass, including backend tests, frontend build, `verify:production-serve --skip-build`, API contract, HTTP smoke, and UI CDP smoke.

## Review Rounds
- Review round 1: inspected Round 190 and selected the documented next risk: single-process production/tunnel deployment.
- Review round 2: inspected FastAPI app route order and confirmed static serving must not swallow `/api/*` or `/mcp/rpc`.
- Review round 3: added frontend build path constants and static file response helpers.
- Review round 4: added root and SPA fallback routes after all API routes.
- Review round 5: added API/mcp guard behavior so missing backend routes do not return HTML.
- Review round 6: added `start:lan` and `verify:production-serve` scripts.
- Review round 7: added production serving verification that checks build, root HTML, SPA fallback, API health, and API 404 isolation.
- Review round 8: added the new command to server-required scope and `verify:full:quick`.
- Review round 9: documented single-process serving in README and the submission checklist.
- Review round 10: ran initial syntax/README/command checks and found the mistaken Node syntax check against Python.
- Review round 11: reran Python syntax correctly and verified production serving directly.
- Review round 12: ran readiness/readme-output checks and confirmed command counts updated.
- Review round 13: ran FastAPI verification to check backend/API/RAG regressions.
- Review round 14: ran full quick verification to check production-serve integration, API contract, HTTP smoke, and UI CDP smoke.

## Issues Found and Resolved
- Issue: using `node --check` on a Python file produced `ERR_UNKNOWN_FILE_EXTENSION`.
- Resolution: reran syntax validation with `python -m py_compile`.
- Issue: a naive catch-all route could serve React HTML for unknown API paths.
- Resolution: catch-all now rejects `api/` and `mcp/` prefixes with API 404.

## Regression Checks
- Existing FastAPI tests still pass: 8 backend tests.
- Existing React build still passes.
- Existing API contract, HTTP smoke, MCP, hub, RAG, run history, scheduler, integration profile, and UI CDP checks still pass.
- Command scope, README output, and readiness output all reflect the new `verify:production-serve` command.

## Next Risks
- Add a deployment artifact checklist for reverse proxy/tunnel environments, including allowed origins, HTTPS, database URL, token secret, and firewall/port expectations.
