# AI Board Evaluation Report - Round 244

## Focus

- 사용자가 지적한 Figma 연결 클릭 시 `Invalid redirect uri`가 뜨는 문제를 우선 수정.
- 원인은 두 가지였음:
  1. 데스크톱 `figma.txt`의 `클라이언트 ID:` 라벨이 OAuth client_id에 그대로 포함됨.
  2. Vite/Cloudflare 프록시를 거친 `/api/oauth/figma/start` 요청에서 백엔드가 실제 외부 origin을 보지 못해 오래된 `.cloudflare-url.txt` 주소를 redirect_uri로 사용함.

## Scores

- Functionality: 9.4/10 - Figma OAuth start URL이 현재 외부 URL callback과 정리된 client_id를 사용함.
- UI/Design: 8.7/10 - 사용자가 누르는 기존 Figma MCP 로그인 버튼 흐름을 유지하면서 내부 API header만 보강함.
- Security/Privacy: 8.8/10 - client_id/client_secret 원문은 응답/로그에 노출하지 않고 검증 출력은 마스킹함.
- Performance: 8.5/10 - OAuth 시작 시 문자열 정리와 header 확인만 추가되어 부하 영향이 없음.
- Tests: 9.3/10 - 백엔드 회귀 테스트와 live external URL 검증 스크립트를 추가하고 실행함.
- Docs: 8.2/10 - 이번 리포트에 원인과 검증 결과를 기록함.
- Next Risks: 7.2/10 - Figma 개발자 앱에 등록된 callback URL이 현재 외부 URL과 다르면 Figma 쪽 설정도 업데이트해야 함.

## Verification

- `$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_api.py -k "figma_oauth_start"` - pass, 2 passed
- `npm run verify:figma-oauth` in live folder - pass
- `npm run verify:figma-oauth` in push clone - pass
- Live external API check against `https://railway-mediterranean-snap-populations.trycloudflare.com` - pass
- `npm run build` - pass
- `npm run verify:text` - pass, 70 files scanned
- `npm run verify:taskory-ui` - pass, no regression in previous Taskory UI work

## Review Rounds

1. Inspected OAuth provider config and confirmed redirect URI was built from `AI_BOARD_PUBLIC_BASE_URL` before current external Host.
2. Queried live `/api/oauth/figma/start` and found `client_id` contained a Korean label prefix while `redirectUri` used stale `cheats-biggest...` URL.
3. Added `clean_oauth_env_value` and applied it to GitHub, Notion, Figma, and Google OAuth client id/secret values.
4. Updated `public_base_url` to accept the frontend-provided `X-AI-Board-Public-Origin` for Cloudflare/ngrok public origins.
5. Added frontend API header `X-AI-Board-Public-Origin: window.location.origin` so proxied API calls can still build the correct OAuth callback.
6. Added backend regression test for stale configured URL + labeled Figma credentials.
7. Added `scripts/verify-figma-oauth-redirect.mjs` and verified live external redirect URI/client_id output.

## Result

- Figma OAuth start now returns:
  - `redirectUri`: `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback`
  - sanitized `client_id` without `클라이언트 ID:` prefix
- If Figma still shows `Invalid redirect uri`, the Figma developer app must have this exact callback URL registered:
  `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback`
