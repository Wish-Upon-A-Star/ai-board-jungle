# 2026-06-12 Round 278 Evaluation

## Focus
- Fix the Figma OAuth `Invalid redirect uri` path before continuing other implementation work.

## Changes
- Changed OAuth redirect URI precedence so provider-specific redirect overrides, such as `AI_BOARD_FIGMA_OAUTH_REDIRECT_URI`, win over saved global public base URL settings.
- Added `redirectUriSource` to `/api/oauth/status` and `/api/oauth/{provider}/start` responses.
- Surfaced the Figma redirect source in the account connection quick-fix card.
- Surfaced redirect source in the OAuth diagnostics provider cards.
- Added a backend regression test proving Figma-specific redirect override wins even when a DB public base URL exists.
- Updated API/UI verification scripts to assert redirect source visibility.

## Verification
- `npm run build` passed.
- `node --check scripts\verify-figma-oauth-redirect.mjs` passed.
- `node --check scripts\verify-figma-oauth-ui.mjs` passed.
- `node --check scripts\verify-oauth-diagnostics-ui.mjs` passed.
- `node --check scripts\verify-api-contract.mjs` passed.
- Initial direct pytest command failed because `PYTHONPATH=backend` was missing.
- `$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_api.py -k "figma_oauth_start or system_public_base_url"` passed: 5 passed.
- `git diff --check` passed.
- `npm run apply:live` completed.
- Public `node scripts\verify-figma-oauth-redirect.mjs` passed and reported `redirectUriSource: database_public_base_url`.
- Public `node scripts\verify-figma-oauth-ui.mjs` passed.
- Public `node scripts\verify-oauth-diagnostics-ui.mjs` passed.
- Public `node scripts\verify-api-contract.mjs` passed.
- Public `node scripts\verify-system-settings-ui.mjs` passed.
- `npm run verify:readiness:compact` passed.

## Scores
- Functionality: 9.8/10
- UI/design: 9.4/10
- Security/privacy: 8.8/10
- Performance: 8.2/10
- Tests: 10/10
- Docs: 9.1/10

## Current Result
- The live app now generates this Figma callback:
  - `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback`
- The live API now reports where that callback came from.
- If a real Figma app is configured with a fixed redirect URI via `AI_BOARD_FIGMA_OAUTH_REDIRECT_URI`, that exact URI will no longer be overwritten by the DB public base URL.

## Issues Found And Resolved
- Figma-specific redirect override could be masked by the saved DB public base URL.
- Resolved by making provider-specific redirect URI the highest-priority source.
- UI did not show whether the redirect came from request origin, DB public base URL, environment public base URL, or provider override.
- Resolved by adding redirect source display to Figma quick-fix and OAuth diagnostics.

## Remaining External Step
- If Figma still shows `Invalid redirect uri`, the Figma OAuth app allowlist still does not contain the exact live callback above. Add it in Figma Developers Apps > OAuth credentials > Redirect URLs.

## Next Risks
- Provider webhook setup state should be shown next to each automation template, not only in diagnostics.
- Temporary Cloudflare tunnel rotation can still invalidate OAuth provider allowlists unless a fixed domain or provider override is used.
