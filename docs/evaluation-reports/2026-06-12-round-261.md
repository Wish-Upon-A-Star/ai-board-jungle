# 2026-06-12 Round 261 Evaluation

## Focus
- Fix the user-facing Figma OAuth `Invalid redirect URI` path first.
- Make the callback URL visible in the profile connection tab, not only inside diagnostics.
- Finish the Cloudflare named tunnel setup/verification work started in the previous loop.

## Changes
- Added a Figma quick-fix panel in `프로필 > 연결하기` with the exact callback URL and a copy button.
- Updated the Figma OAuth UI verification to assert the quick-fix callback is visible before clicking login.
- Added Cloudflare named tunnel setup and verification scripts for stable external domains.
- Updated external serve and network verification to cover named tunnel mode and non-destructive server checks.
- Fixed a stale README assertion in `verify-template-presets`.

## Verification
- `npm run build` passed.
- `npm run verify:figma-oauth` passed.
- `npm run verify:figma-oauth-ui` passed against the live public URL.
- `npm run verify:template-presets` passed.
- `npm run verify:cloudflare-tunnel` passed.
- `npm run verify:network-config` passed.
- `npm run verify:external-serve` passed.
- `npm run verify:readiness:compact` passed, 13/13.
- Live health passed on `http://127.0.0.1:8000/api/health`.
- Public health passed on `https://railway-mediterranean-snap-populations.trycloudflare.com/api/health`.

## Scores
- Functionality: 8.7/10
- UI/design: 8.1/10
- Security/privacy: 8.0/10
- Performance: 8.0/10
- Tests: 8.6/10
- Docs: 8.4/10

## Current Result
- The app now sends Figma this callback URL:
  `https://railway-mediterranean-snap-populations.trycloudflare.com/api/oauth/figma/callback`
- The live UI now shows that URL directly beside the Figma login flow.
- If Figma still shows `Invalid redirect URI`, the remaining required action is external: register that exact URL in the Figma OAuth App Redirect URI allowlist.

## Next Risks
- The current Cloudflare URL is still a temporary quick tunnel. If it changes, all OAuth providers must be updated again.
- A real stable domain requires completing Cloudflare named tunnel creation and setting `AI_BOARD_PUBLIC_BASE_URL` to that domain.
- Figma/Google provider consoles cannot be updated by the app itself; the user must register the exact callback URL in each provider console.
