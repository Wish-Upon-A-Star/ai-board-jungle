# Round 213 Evaluation

## Previous Reports Used

- Round 212: GitHub webhook matching handles alternate repository payload fields. The user's next priority was PostgreSQL-only runtime behavior and a real user-style click playtest against the live server.

## Scope

- Added a runtime guard that rejects SQLite database URLs unless `AI_BOARD_ALLOW_SQLITE_TEST_DB=1` is explicitly set.
- Updated backend tests to use the explicit SQLite test-only flag.
- Added a regression test proving SQLite runtime import fails without the test-only flag.
- Added `scripts/playtest-user-flow.mjs` and `npm run playtest:user` for browser-based user-flow clicking.
- Installed Playwright as a root dev dependency and downloaded Chromium for local playtesting.
- Ran the playtest against the live server at `http://127.0.0.1:3000`.
- Tried to provision PostgreSQL with `winget`; the installer was canceled with `0x800704c7`.

## Scores

- Functionality: 8/10
- UI/design: 9/10
- Security/privacy: 10/10
- Performance: 9/10
- Tests: 10/10
- Docs: 10/10

## Review Rounds

1. Inspected current PostgreSQL defaults and found runtime defaults already point to PostgreSQL.
2. Inspected `backend/app/db.py` and found SQLite runtime fallback code still active.
3. Added a runtime guard to reject SQLite unless a test-only flag is set.
4. Updated backend tests to set `AI_BOARD_ALLOW_SQLITE_TEST_DB=1`.
5. Added a subprocess test proving SQLite import fails without the test-only flag.
6. Verified no PostgreSQL server, Docker, or psql was installed/listening.
7. Tried `npm run setup:postgres`; it reported PostgreSQL/Docker unavailable.
8. Tried `winget install PostgreSQL.PostgreSQL.17 --silent`; it was canceled with `0x800704c7`.
9. Added a reusable Playwright user-flow playtest script.
10. Installed Playwright and Chromium, then ran the playtest against the live `3000/8000` server.
11. Verified the live click flow: register, save profile settings, save integration profile, create automation, run, history, share, scheduler tick, collect RAG, activity filters, API console, delete automation.
12. Re-ran backend tests, backend syntax compilation, README verification, text-output verification, readiness-output verification, and PostgreSQL verification.

## Issues Found And Resolved

- Issue: Runtime could still be pointed at SQLite, which conflicted with PostgreSQL-only operation.
  Resolution: Runtime startup now rejects SQLite unless `AI_BOARD_ALLOW_SQLITE_TEST_DB=1` is explicitly set.
- Issue: There was no reusable browser playtest command for acting like a real user through the UI.
  Resolution: Added `npm run playtest:user`, which drives Chromium through the main UI workflow and emits JSON results plus screenshots.

## Verification

- Pass: `PYTHONPATH=backend python -m pytest backend/tests` with 15 tests passing.
- Pass: `PYTHONPATH=backend python -m py_compile backend/app/db.py backend/app/main.py backend/tests/test_api.py`.
- Pass: `npm --prefix frontend run build`.
- Pass: `npm run verify:readme`.
- Pass: `npm run verify:text-output`.
- Pass: `npm run verify:readiness-output`.
- Pass: `AI_BOARD_PLAYTEST_URL=http://127.0.0.1:3000 npm run playtest:user`.
- Pass: live server remained reachable at `http://127.0.0.1:3000` and `http://127.0.0.1:8000/api/health`.
- Failed environment: `npm run setup:postgres` reports PostgreSQL/Docker unavailable.
- Failed environment: `winget install PostgreSQL.PostgreSQL.17 --silent` canceled with `0x800704c7`.
- Failed environment: `npm run verify:postgres` still fails with `ECONNREFUSED localhost:5432`.

## Live Server Application Status

- The live server was playtested successfully.
- The new PostgreSQL-only backend guard has not been safely applied by restarting the live API because no PostgreSQL server is available. Restarting live FastAPI with the new code and default PostgreSQL URL would make `/api/health` fail until PostgreSQL is installed or `AI_BOARD_DATABASE_URL` points to a reachable PostgreSQL instance.

## Next Risks

- Install/start PostgreSQL or provide a reachable PostgreSQL URL, then restart live FastAPI with the new code and run `npm run verify:postgres`.
- After PostgreSQL is reachable, rerun `npm run playtest:user` against the live server after the live API restart to prove the applied server is using the PostgreSQL-only runtime.
